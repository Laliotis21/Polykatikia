# Transactions domain (Agent 2)

Owns mismatch gate (`mismatch.ts`), create path (`create.ts`), and coordination with Agent 3 anomaly/alert hooks inside one Prisma `$transaction`.

See design §4 integrity rules and implementation Tasks 4 & 6.
